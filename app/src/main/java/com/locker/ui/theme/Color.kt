package com.locker.ui.theme

import androidx.compose.ui.graphics.Color

// Premium Deep Dark Theme Colors
val BackgroundDark = Color(0xFF000000) // Solid black for new logo
val SurfaceDark = Color(0xFF111111) // Slightly lighter for cards
val SurfaceDarkElevated = Color(0xFF222222) // Elevated cards/chips

// Vibrant Accents (Neon)
val PrimaryAccent = Color(0xFF38BDF8) // Electric Blue
val SecondaryAccent = Color(0xFF818CF8) // Indigo/Purple
val ErrorRed = Color(0xFFEF4444) // Soft Red

// We force dark mode colors primarily since it's a premium dark app
val TextPrimary = Color(0xFFFAFAFA)
val TextSecondary = Color(0xFFA1A1AA)
